<!DOCTYPE html>
<html>
	{{#def["particles/head"]}}
	<body>
		{{#def["particles/navigation"]}}
		<main>
			<a name="top"></a>
			<header>
				<!-- <img id="banner"> -->

				<a id="title" href="{{=it.site.url.pathname}}{{!it.board.name}}/">
					<h2>/{{!it.board.name}}/ — {{!it.board.title}}</h2>
					<h4>{{!it.board.subtitle}}</h4>
				</a>

				<a class="btn material-icons" href="{{=it.site.url.pathname}}{{!it.board.name}}/feed/" alt="Лента постов">
					view_list
				</a>
			</header>
			{{? !it.threads.length}}
				<div class="noThreads">Здесь никого, кроме нас, цыплят!</div>
			{{?? }}
				{{~it.threads :thread}}
					<div class="thread">
						{{~[thread.posts[0]] :post}}
							{{#def['particles/post']}}
						{{~ }}
						{{? thread.postCount && thread.omittedPosts}}
							<a class="postCount" href="/{{!it.board.name}}/res/{{!thread.posts.shift().threadNumber}}.html">Всего постов: {{!thread.postCount}}</a>
						{{?? }}
							{{ thread.posts.shift() }}
						{{? }}

						{{~thread.posts :post}}
							{{#def['particles/post']}}
						{{~ }}
					</div>
				{{~ }}
			{{? }}
			{{#def["particles/pagination"]}}
			<a name="bottom"></a>
		</main>

		{{#def['particles/replyForm']}}
		{{#def["particles/widgets"]}}
		{{#def["particles/notifications"]}}
	</body>
</html>
