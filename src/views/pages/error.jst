<!DOCTYPE html>
<html>
	{{#def["particles/head"]}}
	<body>
		{{#def["particles/navigation"]}}
		<main>
			<header>
				<div>{{!it.name}}</div>
				<div>{{!it.message}}</div>
				{{?it.stack}}
					<pre>{{=it.stack}}</pre>
				{{?}}
			</header>
		</main>
		{{#def["particles/widgets"]}}
		{{#def["particles/notifications"]}}
	</body>
</html>
