<div class="postWrapper" id="wrapper-{{!post.id}}">
  <div class="postDetails">
    <!--
      <input type="checkbox" class="hidden" id="delete-{{!post.id}}">
      <label for="delete-{{!post.id}}" class="iconCheckbox checkbox"></label>
    -->
    <span class="info">
      {{? post.sageru}}
        <span class="actionIcon sage"></span>
      {{?}}
      {{? post.subject}}<span class="topic">{{!post.subject}}</span>{{?}}
      <span class="author">{{? post.name}}{{!post.name}}{{??}}Лисичка{{?}}</span>
      <span class="postDate">{{!post.created_at}}</span>
    </span>
    <span class="postMenu">
      <a class="refLink" href="/{{!it.site.pathPrefix}}{{!it.board.name}}/res/{{!post.thread}}.html#{{!post.id}}">№{{!post.id}}</a>
      <span class="actionIcon reply jsonly" onclick="quickReply({{!post.id}}, {{!post.thread}})"></span>
      <span class="actionIcon edit jsOnly"></span>
      <span class="actionIcon delete jsOnly"></span>
      <span class="actionIcon spoil jsOnly" onclick="spoil(this{{? post.id == post.thread}}, true{{?}})"></span>
      <!--<i class="actionIcon source"></i>-->
    </span>
  </div>
  <div class="post">
    <div class="postMedia">
      {{? post.id == post.thread}}
        <a href="/images/nopic.png" target="_blank" onclick="showImage(event, '/images/nopic.png')">
          <img class="preview" src="/images/nopic.png">
          <div class="imgInfo">
            nopic.png, 0MB
          </div>
        </a>
      {{?}}
    </div>
    <div class="postText">
      {{=post.body}}
      {{? post.replies}}
        <div class="postReplies">
          Ответы: {{!post.replies}}
        </div>
      {{?}}
    </div>
  </div>
</div>
