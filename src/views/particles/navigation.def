<input type="checkbox" id="hideSidebarCheckbox" hidden>
<input type="checkbox" id="hideImagesCheckbox" hidden>

<div id="sidebar">
	<div id="logo">
		<a href="/{{!it.site.pathPrefix}}">Тумба.ч</a>
		<a href="/{{!it.site.pathPrefix}}">технологичный и удобный имиджборд</a>
	</div>

	<div id="boards">
		{{~it.boardGroups || [{boards: it.boards, name: "boards", title: "Доски"}] :group}}
			{{~group.boards.filter(function(board) { return !board.hidden; }) :board}}
				<a href="/{{!it.site.pathPrefix}}{{!board.name}}" {{? it.board && board.name == it.board.name}} class="selected"{{? }}>
					/{{!board.name}}/ — {{!board.title}}
				</a>
			{{~ }}
		{{~ }}
	</div>

	<div id="musicPlayer" class="jsOnly">
		<div id="cover" style="background-image: url(https://i.redd.it/3nougoqaj0cx.png)"></div>
		<div id="title">Penpals — Tell Me Why</div>
		<div id="controls">
			<a class="btn material-icons" id="previousTrack">skip_previous</a>
			<a class="btn material-icons" id="pause">play_arrow</a>
			<a class="btn material-icons" id="nextTrack">skip_next</a>
		</div>
		<input id="progress" type="range" min="0" max="100">
	</div>

	<noscript>JavaScript отключен.</noscript>
</div>

<div id="topbar">
	<a href="/{{!it.site.pathPrefix}}">Тумба.ч</a>

	{{~it.boardGroups || [{boards: it.boards, name: "boards", title: "Доски"}] :group}}
		{{~group.boards.filter(function(board) { return !board.hidden; }) :board}}
			<a href="/{{!it.site.pathPrefix}}{{!board.name}}/">/{{!board.name}}/</a>
		{{~ }}
	{{~ }}

	<!--<div id="progressBar"></div>-->
</div>

<div id="navbar">
	<div class="column">
		<a id="toTop" class="btn material-icons" href="#top">arrow_upward</a>
		<a onclick="switchAttribute(sel('.widget#search'), 'hidden')" class="btn material-icons jsOnly">search</a>
		<a onclick="switchAttribute(sel('.widget#preferences'), 'hidden')" class="btn material-icons jsOnly">settings</a>
		<label for="hideSidebarCheckbox" class="btn material-icons">view_quilt</label>
	</div>
	<div class="column">
		<label for="hideImagesCheckbox" class="btn material-icons">visibility_off</label>
		<label for="replyFormShow" id="replyFormButton" class="btn material-icons replyFormShow ">chat</label>
		<a id="toBot" class="btn material-icons" href="#bottom">arrow_downward</a>
	</div>
</div>
