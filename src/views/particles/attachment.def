<a href="{{=it.foxtan.upload}}{{!file.path}}" target="_blank" data-name="{{!file.name}}" data-mime="{{=file.mime}}" onclick="handleAttachmentClick(event)">
	{{? /^(image|video)/.test(file.mime)}}
		{{? file.thumb}}
			<div style="background-image: url({{=it.foxtan.thumb}}{{!file.thumb.path}}); width: {{!file.thumb.width}}px; height: {{!file.thumb.height}}px"></div>
		{{?? }}
			<div class="missingThumbnail">
				{{? file.nsfw}}
					NSFW
				{{?? }}
					Превью недоступно
				{{? }}
			</div>
		{{? }}

		<div>
			<span>{{!file.size}}</span>
			{{? file.duration }}<span>{{!file.duration}}</span>{{?}}
		</div>
	{{?? }}
		{{? file.mime.split('/')[0] === "audio" }}<span class="material-icons">music_note</span>{{? }}
		{{!file.name}} 
		<span>{{!file.size}}</span>
	{{? }}
</a>
