<!DOCTYPE html>
<html lang='{{!it.site.locale}}'>
{{#def['partials/head']}}
<body>
  {{#def['partials/sidebar']}}
  <main>
    {{#def['custom/content/0'] || ''}}
    {{#def['custom/faq'] || def['default/faq'] || ''}}
    {{#def['partials/footer']}}
  </main>
</body>
</html>